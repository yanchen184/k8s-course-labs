# lesson5/service-loadbalancer.yaml — LoadBalancer Service（觀察 EXTERNAL-IP pending）
apiVersion: v1
kind: Service
metadata:
  name: nginx-loadbalancer
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
