# 5-12 排錯練習：selector 故意打錯的 ClusterIP Service
apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
spec:
  type: ClusterIP
  selector:
    app: my-nginx          # BUG：應該是 app: nginx
  ports:
    - port: 80
      targetPort: 80
