# lesson5/httpd-svc.yaml — httpd ClusterIP Service（Loop 4 學員實作）
apiVersion: v1
kind: Service
metadata:
  name: httpd-svc
spec:
  type: ClusterIP
  selector:
    app: httpd
  ports:
    - port: 80
      targetPort: 80
