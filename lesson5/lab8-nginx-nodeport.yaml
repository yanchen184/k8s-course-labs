# lesson5/lab8-nginx-nodeport.yaml — Lab 8：NodePort Service for nginx-web（port 30088）
apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport
spec:
  type: NodePort
  selector:
    app: nginx-web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30088
