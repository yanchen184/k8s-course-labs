# lesson5/lab8-nginx-clusterip.yaml — Lab 8：ClusterIP Service for nginx-web
apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
spec:
  type: ClusterIP
  selector:
    app: nginx-web
  ports:
    - port: 80
      targetPort: 80
