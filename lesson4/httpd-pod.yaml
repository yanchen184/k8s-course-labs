# Lab 1：第一個 Pod
# 部署一個 nginx，用 port-forward 在瀏覽器看到頁面
apiVersion: v1
kind: Pod
metadata:
  name: my-httpd
  labels:
    app: nginx
spec:
  containers:
    - name: my-httpd
      image: httpd:2.4
      ports:
        - containerPort: 80
