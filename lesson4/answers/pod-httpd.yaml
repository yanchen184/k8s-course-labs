# Lab 5：自由練習 — 換一個不同的 image
# 把 nginx 換成 httpd，驗證你真的會寫 Pod YAML
# 挑戰：進容器後用什麼指令看到 httpd 的歡迎頁面？
apiVersion: v1
kind: Pod
metadata:
  name: my-httpd
  labels:
    app: httpd
spec:
  containers:
    - name: httpd
      image: httpd:2.4
      ports:
        - containerPort: 80
